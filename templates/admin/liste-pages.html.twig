{% extends 'base.html.twig' %}
{% block title %}
	Liste des pages
{% endblock %}
{% block body %}

	<h1 class="titre">Liste des pages</h1>
	{% if listePages is defined and listePages is not null %}

		<table>
			<thead>
				<tr>
					<th></th>
					<th>Titre</th>
					<th>Sous titre</th>
					<th>Etat</th>
					<th>Créée le</th>
					<th>Modifiée le</th>
				</tr>
			</thead>
			<tbody>
				{% for page in listePages %}
					<tr>
						<td>
							<a href="{{path('pages_modifier_page',{'id':page.id})}}">Modifier</a>
						</td>
						<td>{{page.titre}}</td>
						<td>{{page.soustitre}}</td>
						<td>{{page.etat}}</td>
						<td>{{page.createdat|date('d-m-Y H:i:s')}}</td>
						<td>{{page.updatedat|date('d-m-Y H:i:s')}}</td>
					</tr>
				{% endfor %}
			{% else %}
				<p>Aucune donnée à afficher.</p>
			{% endif %}
		</tbody>
	</table>
{% endblock %}
